import React from 'react';
import ReactDOM from 'react-dom';
import FilterChip from './FilterChip';

it('It should mount', () => {
  const div = document.createElement('div');
  // ReactDOM.render(<FilterChip />, div);
  ReactDOM.unmountComponentAtNode(div);
});